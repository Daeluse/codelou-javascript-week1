/**
 * The Number type in JavaScript is one of the three 'Primitive' types.
 * It is used to convey various things, but a common use case is to identify
 * the amount of something. E.G. how many characters are in a string.
 * 
 * Example: 74
 * 
 * To run this suite, you can execute `npm run test:numbers` from the root folder.
 */

class NumberSuite {

    /**
     * Challenge 1, how can we convert a string to a number?
     * 
     * Hint: JavaScript has a global type called Number. In the example
     * below, you can use your intellisense on this type to explore available
     * methods. Note that in many cases Numbers are reffered to as Int in JS.
     * 
     * @param {String} numberString 
     */
    convertToNumber(numberString) {
        // Use this line as an additional hint when completing this challenge (casing matters).
        // return Number.<METHOD_NAME>(numberString);
    }

    /**
     * Challenge 2, how can we calculate and return the length of something?
     * 
     * Hint: The answer lies in the question.
     * 
     * @param {String} string
     */
    calculateStringLength(string) {

    }

}

module.exports = NumberSuite;